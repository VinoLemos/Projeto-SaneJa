import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#3b8786",
    },
    secondary: {
      main: "rgba(252, 252, 252, 0.2)",
    },
  },
});

export default Theme;
